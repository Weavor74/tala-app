# src/renderer/catalog/ — A2UI Component Catalog

Contains the component definitions used by the `A2UIRenderer` dynamic rendering system.

---

## Files

| File | Size | Description |
|---|---|---|
| `BasicComponents.tsx` | 2 KB | **Basic UI Components.** Implements primitive A2UI components (`Text`, `Button`, `Card`, `Input`, `Image`, `List`) that the `RecursiveRenderer` maps to when parsing agent-generated JSON UI structures. |
